---
weight: 100
title: API Reference
type: docs
---


- [`Axial.Result`](./result/): Result combinators, extraction helpers, traversal, and the `result { }`,
  `result.list { }`, and `result.array { }` computation expressions.

`Axial.Result` is a standalone leaf and installs on its own. The Constraint, Refined, and Parse references live under
[Values]({{< relref "/values/reference/" >}}).
